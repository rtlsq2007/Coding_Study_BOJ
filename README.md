# 📘 Algorithm Study Log

꾸준히 백준 문제를 풀고,  
문제 설명 / 풀이 아이디어 / 배운 점 / 느낀 점을 정리하는 공간입니다.

---

<details>
<summary>📅 2026-03-01 (Step 3. 반복문)</summary>

<br>

## ✅ 10950 - A+B - 3  
**문제 유형:** 반복문, 입출력  

### 📌 문제 설명  
테스트 케이스 T가 주어지고, 각 줄마다 두 정수 A와 B가 주어진다.  
각 테스트 케이스마다 A+B를 출력하는 문제이다.

### 💡 풀이 설명  
반복문을 사용하여 입력을 받고, 입력받은 값을 즉시 계산하여 출력하는 방식으로 해결했다.  
처음에는 배열에 저장하려 했지만, 이 문제는 저장 없이 바로 처리하는 구조가 더 효율적이라는 것을 알게 되었다.

### 🧠 배운 점  
- 반복문을 활용한 입력 처리  
- 불필요한 배열 사용을 줄이는 사고방식  
- 단순한 구조로 문제를 바라보는 연습  

### ✍ 느낀 점  
문제를 괜히 복잡하게 생각하지 말고, 가장 단순한 해결 방법을 먼저 떠올리는 것이 중요하다고 느꼈다.

---

## ✅ 8393 - 합  
**문제 유형:** 반복문, 누적합  

### 📌 문제 설명  
1부터 n까지의 합을 구하는 문제이다.

### 💡 풀이 설명  
1부터 n까지 반복문을 실행하며 누적 합을 계산하였다.

### 🧠 배운 점  
- 누적합 패턴 이해 (`sum += i`)  
- 반복문의 범위 설정 중요성  

### ✍ 느낀 점  
기초적인 문제지만 반복문 개념을 다시 점검할 수 있었다.

---

## ✅ 25304 - 영수증  
**문제 유형:** 반복문, 조건문  

### 📌 문제 설명  
총 금액과 물건의 가격 및 개수가 주어질 때, 계산한 총합이 실제 총 금액과 일치하는지 확인하는 문제이다.

### 💡 풀이 설명  
각 물건의 가격과 개수를 곱한 값을 누적하여 합계를 구하고, 최종 합과 총 금액을 비교하였다.

### 🧠 배운 점  
- 반복문을 활용한 누적 계산  
- 조건문을 통한 값 비교  
- 자료형 범위에 대한 고려 필요성  

### ✍ 느낀 점  
단순 계산 문제라도 자료형과 변수 초기화는 항상 신경 써야 한다는 것을 느꼈다.

---

## ✅ 25314 - 코딩은 체육과목 입니다  
**문제 유형:** 반복문, 문자열 출력  

### 📌 문제 설명  
정수 N이 주어질 때, N/4 만큼 "long"을 출력하고 마지막에 "int"를 출력하는 문제이다.

### 💡 풀이 설명  
정수 나눗셈을 이용하여 반복 횟수를 계산하고, 반복 출력 후 마지막에 int를 출력하였다.

### 🧠 배운 점  
- 반복문을 활용한 문자열 출력  
- 정수 나눗셈의 활용  

### ✍ 느낀 점  
출력 형식을 정확히 맞추는 것이 중요하다는 것을 다시 느꼈다.

<br>
</details>
<details>
<summary>📅 2026-03-02 (Step 3. 반복문 6~9번)</summary>

<br>

## ✅ 15552 - 빠른 A+B  
**문제 유형:** 반복문, 빠른 입출력  

### 📌 문제 설명  
테스트 케이스 T가 매우 크게 주어질 때, 각 줄마다 두 정수를 입력받아 합을 출력하는 문제이다.  
일반적인 입력 방식으로는 시간 초과가 발생할 수 있다.

### 💡 풀이 설명  
기존 A+B 문제와 구조는 동일하지만, 입력 개수가 많기 때문에  
`ios::sync_with_stdio(false);`와 `cin.tie(nullptr);`를 사용하여 입출력 속도를 개선하였다.  
또한 `endl` 대신 `"\n"`을 사용하여 불필요한 flush를 방지하였다.

### 🧠 배운 점  
- C++에서 입출력 속도 최적화 방법  
- sync 해제와 tie 해제의 역할  
- 시간 초과는 로직 문제가 아니라 입출력 문제일 수도 있다는 점  

### ✍ 느낀 점  
단순한 문제처럼 보였지만, 입출력 최적화의 중요성을 배울 수 있었다.

---

## ✅ 11021 - A+B - 7  
**문제 유형:** 반복문, 형식 출력  

### 📌 문제 설명  
각 테스트 케이스마다 A+B를 구하되,  
출력 형식을 `Case #x: 결과` 형태로 출력해야 하는 문제이다.

### 💡 풀이 설명  
반복문을 통해 입력을 받고,  
각 줄마다 형식에 맞춰 문자열과 값을 조합하여 출력하였다.  
출력 형식에서 공백 위치가 매우 중요했다.

### 🧠 배운 점  
- 출력 형식을 정확히 맞추는 것의 중요성  
- 문자열과 변수 값을 `<<` 연산자로 연결하는 방법  
- 문제에서 요구하는 형식을 그대로 따르는 습관  

### ✍ 느낀 점  
로직은 간단하지만, 출력 형식 하나 때문에 오답이 날 수 있다는 것을 배웠다.

---

## ✅ 11022 - A+B - 8  
**문제 유형:** 반복문, 형식 출력  

### 📌 문제 설명  
11021과 유사하지만,  
`Case #x: A + B = 결과` 형식으로 출력해야 한다.

### 💡 풀이 설명  
입력받은 A와 B를 그대로 출력 형식에 포함시켜야 하므로  
변수를 문자열 안에 넣지 않고, `<<` 연산자를 사용하여 순서대로 출력하였다.

### 🧠 배운 점  
- 변수는 따옴표 안에 넣는 것이 아니라, 출력 연산자로 연결해야 한다는 점  
- 문자열과 숫자 출력을 자연스럽게 조합하는 방법  

### ✍ 느낀 점  
출력 문제는 사소해 보이지만, 정확성이 매우 중요하다는 것을 다시 느꼈다.

---

## ✅ 2438 - 별 찍기 - 1  
**문제 유형:** 반복문, 이중 반복문  

### 📌 문제 설명  
정수 N이 주어질 때,  
1개부터 N개까지 별을 출력하는 문제이다.

### 💡 풀이 설명  
바깥 반복문으로 줄 수를 제어하고,  
안쪽 반복문으로 해당 줄에 출력할 별의 개수를 제어하였다.

### 🧠 배운 점  
- 이중 반복문의 구조 이해  
- 반복 횟수를 현재 줄 번호와 연결하는 사고  

### ✍ 느낀 점  
반복문의 흐름을 정확히 이해하면 별 찍기 문제는 일정한 패턴이라는 것을 알게 되었다.

<br>

</details>
<details>
<summary>📅 2026-03-04 (Step 3. 반복문 10~12번)</summary>

<br>

## ✅ 2439 - 별 찍기 - 2  
**문제 유형:** 반복문, 이중 반복문, 출력 패턴

### 📌 문제 설명
정수 N이 주어졌을 때 별(*)을 이용하여 오른쪽 정렬된 삼각형을 출력하는 문제이다.

### 💡 풀이 설명
각 줄마다 출력해야 하는 **공백과 별의 개수 규칙**을 찾아 구현하였다.

- i번째 줄의 공백 개수 → `N - i`
- i번째 줄의 별 개수 → `i`

따라서 한 줄을 출력할 때  
1. 공백을 먼저 출력하고  
2. 별을 출력하도록 구현하여 오른쪽 정렬을 만들었다.

### 🧠 배운 점
- 이중 반복문을 활용한 출력 패턴 구현  
- 공백과 별의 개수를 계산하여 정렬을 만드는 방법  
- 규칙을 찾으면 별 찍기 문제는 구조가 비슷하다는 점

### ✍ 느낀 점
처음에는 공백의 개수를 계산하는 부분이 헷갈렸지만 규칙을 이해하고 나니 별 찍기 문제는 반복문 패턴 문제라는 것을 알게 되었다.

---

## ✅ 10952 - A+B - 5  
**문제 유형:** 반복문, 종료 조건

### 📌 문제 설명
두 정수 A와 B를 계속 입력받아 합을 출력하는 문제이다.  
단, 입력이 `0 0`일 경우 프로그램을 종료한다.

### 💡 풀이 설명
무한 반복문을 사용하여 입력을 계속 받고,  
`A == 0 && B == 0` 조건이 만족되면 반복을 종료하도록 구현하였다.

각 입력마다 두 수의 합을 계산하여 출력하였다.

### 🧠 배운 점
- 반복문 안에서 종료 조건을 처리하는 방법  
- `break`를 이용한 반복문 종료  
- 입력이 특정 조건일 때 프로그램 흐름을 제어하는 방법

### ✍ 느낀 점
반복문을 사용할 때 종료 조건을 어떻게 설정하는지가 중요하다는 것을 배웠다.

---

## ✅ 10951 - A+B - 4  
**문제 유형:** 반복문, EOF 입력 처리

### 📌 문제 설명
두 정수 A와 B를 입력받아 합을 출력하는 문제이다.  
입력의 개수는 정해져 있지 않으며 **입력이 끝날 때까지 계속 처리해야 한다.**

### 💡 풀이 설명
입력 개수가 주어지지 않기 때문에  
입력이 정상적으로 들어오는 동안 반복문을 실행하도록 구현하였다.

C++에서는 `cin >> a >> b` 가 성공하는 동안 반복하도록 작성하면  
입력이 끝나는 순간 반복문이 종료된다.

### 🧠 배운 점
- 입력의 개수가 정해지지 않은 경우 처리 방법  
- EOF(End Of File) 기반 반복문 사용  
- 문제마다 입력 종료 방식이 다르다는 점

### ✍ 느낀 점
비슷한 A+B 문제라도 입력 종료 방식이 서로 다르다는 것을 알게 되었고, 문제의 입력 조건을 정확히 읽는 것이 중요하다는 것을 느꼈다.

<br>

</details>
<details>
<summary>📅 2026-03-05 (Step 6. 배열 1~6번)</summary>

## ✅ 10807 - 개수 세기

**문제 유형:** 배열, 반복문, 조건문

### 📌 문제 설명

N개의 정수가 주어지고, 찾고자 하는 정수 V가 주어질 때,  
배열에서 V가 몇 번 등장하는지 개수를 출력하는 문제이다.

### 💡 풀이 설명

배열에 N개의 정수를 저장한 후, 반복문을 순회하며  
각 원소가 V와 같을 경우 카운터를 증가시키는 방식으로 해결하였다.

### 🧠 배운 점

* 배열을 순회하며 특정 값을 탐색하는 방법
* 조건문과 카운터 변수를 조합하는 패턴

### ✍ 느낀 점

단순하지만 배열 탐색의 기본 패턴을 익힐 수 있었다.

---

## ✅ 10871 - X보다 작은 수

**문제 유형:** 배열, 반복문, 조건문

### 📌 문제 설명

N개의 정수와 정수 X가 주어질 때,  
배열에서 X보다 작은 수를 순서대로 출력하는 문제이다.

### 💡 풀이 설명

배열을 반복문으로 순회하며, 각 원소가 X보다 작을 경우  
즉시 출력하는 방식으로 해결하였다.

### 🧠 배운 점

* 조건을 만족하는 원소만 필터링하여 출력하는 방법
* 배열에 저장하지 않고 입력과 동시에 조건을 처리하는 구조

### ✍ 느낀 점

배열을 꼭 다 저장하지 않아도 되는 경우가 있다는 것을 다시 느꼈다.

---

## ✅ 10818 - 최솟값과 최댓값

**문제 유형:** 배열, 반복문, 최솟값/최댓값 탐색

### 📌 문제 설명

N개의 정수가 주어질 때, 최솟값과 최댓값을 출력하는 문제이다.

### 💡 풀이 설명

배열의 첫 번째 원소로 min과 max를 초기화한 뒤,  
반복문을 순회하며 현재 값이 max보다 크면 max를 갱신하고,  
min보다 작으면 min을 갱신하는 방식으로 해결하였다.  
처음에는 min과 max를 0으로 초기화하거나 하나의 if/else로 처리하려 했지만,  
두 조건이 완전히 독립적으로 비교되어야 한다는 것을 깨달았다.

### 🧠 배운 점

* min과 max는 서로 독립적으로 비교되어야 한다는 점
* 초기값 설정의 중요성 (0이 아닌 arr[0]으로 초기화)
* 불필요한 루프를 줄여 하나의 반복문에서 처리하는 최적화

### ✍ 느낀 점

로직이 단순해 보여도 초기값과 비교 조건 하나하나가 결과에 큰 영향을 미친다는 것을 느꼈다.

---

## ✅ 2562 - 최댓값

**문제 유형:** 배열, 반복문, 최댓값 탐색, 인덱스

### 📌 문제 설명

9개의 서로 다른 자연수가 주어질 때,  
최댓값과 그 값이 몇 번째 수인지를 출력하는 문제이다.

### 💡 풀이 설명

배열의 첫 번째 원소로 max를 초기화하고, 반복문으로 순회하며  
현재 값이 max보다 크면 max와 해당 인덱스(i+1)를 함께 갱신하는 방식으로 해결하였다.

### 🧠 배운 점

* 최댓값과 인덱스를 동시에 추적하는 방법
* 의미 있는 변수명(max, maxIdx)의 중요성
* 불필요한 변수와 배열을 줄이는 사고방식

### ✍ 느낀 점

처음에 불필요한 변수를 많이 사용했는데, 단순하게 생각하면 코드가 훨씬 깔끔해진다는 것을 다시 느꼈다.

---

## ✅ 10810 - 공 넣기

**문제 유형:** 배열, 반복문, 구간 덮어쓰기

### 📌 문제 설명

N개의 바구니와 M번의 명령이 주어진다.  
각 명령 `i j k`마다 i번부터 j번 바구니까지 k번 공으로 덮어쓰는 문제이다.

### 💡 풀이 설명

M번의 명령을 반복문으로 처리하며,  
각 명령의 범위(from~to)에 해당하는 배열 인덱스를 k로 덮어쓰는 방식으로 해결하였다.  
배열이 1-based 인덱스라는 점을 고려하여 크기를 101로 선언하였다.

### 🧠 배운 점

* 구간 단위로 배열을 업데이트하는 방법
* 1-based 인덱스 처리 방식
* 의미 있는 변수명(from, to, ball)의 중요성

### ✍ 느낀 점

배열 인덱스가 0-based인지 1-based인지를 항상 먼저 확인하는 습관이 필요하다고 느꼈다.

---

## ✅ 10813 - 공 바꾸기

**문제 유형:** 배열, 반복문, swap

### 📌 문제 설명

N개의 바구니에 처음엔 i번 바구니에 i번 공이 들어있다.  
M번의 명령 `i j`마다 i번과 j번 바구니의 공을 서로 교환하는 문제이다.

### 💡 풀이 설명

먼저 N개의 바구니를 `basket[i] = i`로 초기화한 뒤,  
M번의 명령마다 임시 변수(temp)를 활용하여 두 바구니의 값을 교환하는 방식으로 해결하였다.  
초기화 루프 / 교환 루프 / 출력 루프를 명확히 분리하여 구조화하였다.

### 🧠 배운 점

* swap 패턴에서 임시 변수(temp)의 역할
* 초기화 / 처리 / 출력을 분리하는 코드 구조화 습관
* 초기화 위치가 잘못되면 예상치 못한 버그가 생긴다는 점

### ✍ 느낀 점

로직 자체는 단순하지만 초기화 순서 하나가 버그를 만든다는 것을 직접 경험하며 배웠다.

</details>
<details>
<summary>📅 2026-03-06 (Step 6. 배열 7~10번)</summary>

<br>

<details>
<summary><strong>5597 - 과제 안 내신 분..?</strong></summary>

<br>

🔗 문제 링크  
https://www.acmicpc.net/problem/5597

📌 문제 설명  
총 30명의 학생 중 과제를 제출한 학생의 번호가 28개 주어진다.  
과제를 제출하지 않은 두 학생의 번호를 찾아 출력하는 문제이다.

💡 풀이 아이디어  
학생 번호를 저장할 배열을 만들고 제출한 학생 번호를 체크한다.  
이후 배열을 순회하면서 제출되지 않은 번호를 출력한다.

🧠 배운 점
- 배열을 이용한 상태 체크
- 특정 값 존재 여부 확인

✍ 느낀 점  
배열을 활용해 특정 값을 체크하는 기본 패턴을 익힐 수 있었다.

</details>

---

<details>
<summary><strong>3052 - 나머지</strong></summary>

<br>

🔗 문제 링크  
https://www.acmicpc.net/problem/3052

📌 문제 설명  
10개의 수를 입력받아 각각을 42로 나눈 나머지를 구하고  
서로 다른 나머지의 개수를 출력하는 문제이다.

💡 풀이 아이디어  
각 숫자를 42로 나눈 나머지를 배열에 저장한 뒤  
중복되지 않는 값의 개수를 계산하였다.

🧠 배운 점
- 나머지 연산 `%`
- 배열을 이용한 중복 확인

✍ 느낀 점  
배열을 활용한 중복 탐색 방법을 연습할 수 있었다.

</details>

---

<details>
<summary><strong>10811 - 바구니 뒤집기</strong></summary>

<br>

🔗 문제 링크  
https://www.acmicpc.net/problem/10811

📌 문제 설명  
바구니 번호가 순서대로 들어 있는 배열에서  
주어진 구간을 여러 번 뒤집은 후 최종 상태를 출력하는 문제이다.

💡 풀이 아이디어  
양쪽 인덱스를 swap하고 `_i++`, `_j--` 방식으로  
구간 전체를 reverse하였다.

🧠 배운 점
- 배열 reverse 방식
- swap을 이용한 값 교환

✍ 느낀 점  
구간을 뒤집는 로직을 이해하는 데 도움이 되는 문제였다.

</details>

---

<details>
<summary><strong>1546 - 평균</strong></summary>

<br>

🔗 문제 링크  
https://www.acmicpc.net/problem/1546

📌 문제 설명  
시험 점수 중 최댓값을 기준으로 점수를 조정한 뒤  
새로운 평균을 계산하는 문제이다.

💡 풀이 아이디어  
최댓값을 찾은 후 `(점수 / 최댓값) * 100`으로 점수를 변환하고  
평균을 계산하였다.

🧠 배운 점
- 배열에서 최댓값 찾기
- 실수 계산과 형변환

✍ 느낀 점  
정수 나눗셈과 실수 계산의 차이를 이해할 수 있었다.

</details>

</details>
<details>
<summary>📅 2026-03-08 (Step 7. 문자열 1~5번)</summary>

## ✅ 27866 - 문자와 문자열

**문제 유형:** 문자열, 인덱스

### 📌 문제 설명

문자열 S와 정수 i가 주어질 때, S의 i번째 문자를 출력하는 문제이다.

### 💡 풀이 설명

string으로 입력을 받은 뒤, 배열처럼 인덱스로 접근하여 i-1번째 문자를 출력하였다.  
string은 배열과 동일하게 0부터 시작하는 인덱스를 사용하므로 `s[i-1]`로 접근하였다.

### 🧠 배운 점

* string은 char 배열처럼 인덱스로 접근 가능하다는 점
* string 인덱스가 0부터 시작한다는 점

### ✍ 느낀 점

string이 배열과 동일한 방식으로 동작한다는 것을 알게 되었고, 자연스럽게 string에 익숙해질 수 있었다.

---

## ✅ 2743 - 단어 길이 재기

**문제 유형:** 문자열, 길이

### 📌 문제 설명

영어 소문자로만 이루어진 단어를 입력받아 길이를 출력하는 문제이다.

### 💡 풀이 설명

string으로 입력을 받은 뒤, `s.length()`를 사용하여 길이를 출력하였다.  
처음에는 `strlen()`을 사용하였으나 `<cstring>` 헤더가 필요하다는 것을 알게 되었고,  
`string`과 `s.length()`를 사용하는 방식이 더 C++답다는 것을 배웠다.

### 🧠 배운 점

* `strlen()`은 `<cstring>` 헤더가 필요하다는 점
* C++에서는 `char` 배열보다 `string`을 사용하는 것이 더 안전하고 자연스럽다는 점
* `s.length()`와 `s.size()`는 동일한 기능을 한다는 점

### ✍ 느낀 점

단순한 문제였지만 C 스타일과 C++ 스타일의 차이를 처음으로 체감할 수 있었다.

---

## ✅ 9086 - 문자열

**문제 유형:** 문자열, 인덱스

### 📌 문제 설명

테스트케이스 T가 주어지고, 각 문자열의 첫 번째와 마지막 문자를 이어붙여 출력하는 문제이다.

### 💡 풀이 설명

결과를 string 배열에 저장한 뒤 한 번에 출력하는 방식으로 해결하였다.  
중간 문자를 `erase(1, s.length()-2)`로 삭제하여 첫 글자와 마지막 글자만 남기는 방식을 사용하였다.  
단, 문자열 길이가 1인 경우 `s.length()-2`가 unsigned 타입 특성상 오버플로우가 발생하므로  
길이가 1일 때는 해당 문자를 두 번 붙이는 예외 처리를 추가하였다.

### 🧠 배운 점

* `string::length()`가 unsigned 타입을 반환하므로 음수 계산 시 오버플로우가 발생할 수 있다는 점
* `erase(시작, 길이)` 로 문자열 중간을 삭제하는 방법
* 엣지 케이스(길이가 1인 문자열)를 항상 고려해야 한다는 점

### ✍ 느낀 점

동작은 맞는 것 같아도 예외 케이스 하나 때문에 틀릴 수 있다는 것을 직접 경험했다.  
코드를 작성할 때 항상 극단적인 입력값을 먼저 생각해보는 습관이 필요하다고 느꼈다.

---

## ✅ 11654 - 아스키 코드

**문제 유형:** 문자, 아스키 코드, 형변환

### 📌 문제 설명

알파벳 소문자, 대문자, 숫자 0~9 중 하나가 주어질 때, 주어진 문자의 아스키 코드 값을 출력하는 문제이다.

### 💡 풀이 설명

`char` 타입으로 입력받은 뒤, `(int)`로 형변환하여 아스키 코드 값을 출력하였다.  
아스키 코드 값을 직접 외우거나 지정할 필요 없이, C++에서 char를 int로 변환하면 자동으로 아스키 코드 값이 반환된다.

### 🧠 배운 점

* `char`를 `int`로 형변환하면 아스키 코드 값이 반환된다는 점
* 아스키 코드의 개념과 문자-숫자 변환 방식

### ✍ 느낀 점

데이터가 결국 숫자로 저장된다는 것을 코드로 직접 확인할 수 있어서 흥미로웠다.

---

## ✅ 11720 - 숫자의 합

**문제 유형:** 문자열, 형변환, 누적합

### 📌 문제 설명

N개의 숫자가 공백 없이 이어진 문자열로 주어질 때, 각 자리 숫자의 합을 출력하는 문제이다.  
숫자가 너무 커서 일반 정수형으로 입력받을 수 없기 때문에 문자열로 입력받아야 한다.

### 💡 풀이 설명

숫자를 string으로 입력받은 뒤, 반복문으로 각 문자에 접근하여  
`s[i] - '0'`을 통해 char를 int로 변환하고 누적 합산하는 방식으로 해결하였다.  
`'0'`의 아스키 코드(48)를 빼면 해당 숫자 문자의 실제 정수값이 나온다는 원리를 활용하였다.

### 🧠 배운 점

* 큰 수는 정수형 대신 string으로 입력받아야 한다는 점
* `s[i] - '0'`으로 char를 int로 변환하는 방법
* 아스키 코드를 활용한 문자-숫자 변환 원리

### ✍ 느낀 점

입력값의 범위를 항상 먼저 확인해야 한다는 것을 배웠다.  
단순해 보이는 문제도 자료형 선택이 잘못되면 틀릴 수 있다는 것을 느꼈다.

</details>
